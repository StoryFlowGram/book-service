apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-book-data
  namespace: default
  labels:
    app: postgres-book
    env: production
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi